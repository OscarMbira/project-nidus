/**
 * PMO Project Oversight — Change Register (all projects).
 * Uses full-page CRUD forms (no modals).
 */
import ChangeRequests from '../change/ChangeRequests'

export default function PMOOversightChangeRegister() {
  return <ChangeRequests formVariant="page" />
}
