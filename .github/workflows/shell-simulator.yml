name: Simulator Shell — Build & Deploy

on:
  push:
    branches: [master, main]
    paths:
      - 'apps/simulator/**'
      - 'pnpm-lock.yaml'
      - 'turbo.json'
      - 'pnpm-workspace.yaml'

  pull_request:
    paths:
      - 'apps/simulator/**'

jobs:
  build-shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm turbo build --filter=@nidus/simulator-app
        env:
          VITE_SUPABASE_URL: ${{ secrets.SIMULATOR_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.SIMULATOR_SUPABASE_ANON_KEY }}
          VITE_FEDERATION_ENABLED: ${{ vars.VITE_FEDERATION_ENABLED || 'false' }}
          VITE_MODULE_SIM_PLANNING_URL: ${{ secrets.MODULE_SIM_PLANNING_URL }}
          VITE_MODULE_SIM_RISK_URL: ${{ secrets.MODULE_SIM_RISK_URL }}
          VITE_MODULE_SIM_QUALITY_URL: ${{ secrets.MODULE_SIM_QUALITY_URL }}
          VITE_MODULE_SIM_PMO_URL: ${{ secrets.MODULE_SIM_PMO_URL }}
          VITE_MODULE_SIM_SCENARIOS_URL: ${{ secrets.MODULE_SIM_SCENARIOS_URL }}
          VITE_MODULE_SIM_LEADERBOARD_URL: ${{ secrets.MODULE_SIM_LEADERBOARD_URL }}
          VITE_MODULE_SIM_ADMIN_URL: ${{ secrets.MODULE_SIM_ADMIN_URL }}
          TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
          TURBO_TEAM: ${{ secrets.TURBO_TEAM }}
      - name: Upload simulator shell artifact
        uses: actions/upload-artifact@v4
        with:
          name: simulator-shell-dist
          path: apps/simulator/dist
      - name: Deploy to Vercel (Simulator Shell)
        if: github.event_name == 'push' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main')
        run: pnpm dlx vercel deploy apps/simulator/dist --prod --yes --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_PROJECT_ID: ${{ secrets.SIMULATOR_VERCEL_PROJECT_ID }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
