# v339 — Progressive Web App (PWA) Full Implementation Plan
## Platform & Simulator

**Date:** 2026-03-28  
**Scope:** Platform + Simulator (both domains)  
**Prerequisite:** None — this is a cross-cutting infrastructure improvement  
**Status:** **Implementation complete** (codebase audited 2026-03-28)

---

## 1. Current State Assessment

### Post-implementation (2026-03-28)

| Asset | File | Status |
|---|---|---|
| Web manifest | `public/manifest.json` | **Done** — icons, dark `background_color`, `id`, `lang`, `dir`, shortcuts (Platform + Simulator including `/simulator/scenarios`) |
| Service worker | Generated by `vite-plugin-pwa` / Workbox | **Done** — `registerType: 'prompt'`, precache, `navigateFallback: /offline.html`, `push-notifications-sw.js` imported |
| PWA utility helpers | `src/utils/pwaUtils.js` | **Done** — install helpers; SW registration deferred to plugin (`getRegistration`) |
| Install prompt | `src/components/PWAInstallPrompt.jsx` | **Done** — engagement heuristic (3+ paths), iOS/Android/desktop instructions, installed detection |
| Offline / update UI | `src/components/pwa/*` | **Done** — `OfflineIndicator`, `PWAUpdatePrompt` (`virtual:pwa-register/react`) |
| Offline page | `public/offline.html` | **Done** — branded fallback |
| Vite / Workbox | `vite.config.js` | **Done** — `VitePWA`, runtime caching, Supabase network-only, background sync queue names for tasks/issues/risks/defects |
| PWA settings | `src/pages/app/PWASettings.jsx` | **Done** — install, push toggle, storage, cache clear, connection/sync copy |
| Push infra | `SQL/v353_push_subscriptions_table.sql`, `src/services/pushNotificationService.js`, `PushNotificationToggle.jsx` | **Done** — client subscription storage; server send is separate |
| Offline queue hook | `src/hooks/useOfflineQueue.js` | **Done** — used on task/issue/risk flows |
| Simulator parity | Same origin + routes | **Done** — `/simulator/scenarios` redirects to `/simulator/practice-projects`; manifest shortcut added |

---

## 2. Architecture Recommendation

*(Unchanged — see original design.)*  
**Chosen approach:** `vite-plugin-pwa` + Workbox.

---

## 3. Implementation Phases & Todo List

### Phase 1: Package Installation & Vite Config
- [x] Install `vite-plugin-pwa` and `workbox-window` as dependencies
- [x] Add `VitePWA` plugin to `vite.config.js` with `registerType: 'prompt'`
- [x] Configure Workbox `runtimeCaching` rules per the strategy table above
- [x] Set `devOptions: { enabled: false }` so SW does not interfere in dev mode
- [x] Remove hand-rolled `public/service-worker.js` (replaced by build-time generated SW)

### Phase 2: Manifest & Icons
- [x] Fix `public/manifest.json` (icons, `background_color`, `display_override`, `id`, shortcuts, `lang`, `dir`, `prefer_related_applications`)
- [x] Create/verify proper icon PNG files at required sizes (incl. `maskable-icon-512.png`, `apple-touch-icon-180.png`, favicons)
- [x] Add `apple-touch-icon` link tags to `index.html`
- [x] Add splash / status-bar meta (`apple-mobile-web-app-status-bar-style`, etc.)
- [x] Add `msapplication-TileColor` meta for Windows tiles

### Phase 3: Offline Experience
- [x] Create `public/offline.html` — branded offline page
- [x] Configure SW to serve `offline.html` for navigation failures (`navigateFallback`)
- [x] Create `src/components/pwa/OfflineIndicator.jsx`
- [x] Wire `OfflineIndicator` into `App.jsx`
- [x] Add online/offline event listeners

### Phase 4: Update Notification
- [x] Create `src/components/pwa/PWAUpdatePrompt.jsx`
- [x] Use `useRegisterSW` from `virtual:pwa-register/react`
- [x] Wire `PWAUpdatePrompt` into `App.jsx`
- [x] Update `src/utils/pwaUtils.js` — no manual SW registration; plugin + `getRegistration` pattern

### Phase 5: Enhanced Install Prompt
- [x] Update `PWAInstallPrompt.jsx` (engagement heuristic, multi-platform instructions, “already installed” behavior)
- [x] Add persistent install entry via **App & install** — `PWASettings` linked from Platform/Simulator headers (`/platform/pwa-settings`, `/simulator/pwa-settings`)

### Phase 6: Background Sync (Offline Form Submissions)
- [x] Configure Workbox background sync queue names for tasks, issues, risks, defects (see `vite.config.js` `pwaRuntimeCaching`)
- [x] Create `src/hooks/useOfflineQueue.js`
- [x] Integrate `useOfflineQueue` on key flows (e.g. `TasksCreate`, issue/risk register views)

### Phase 7: Push Notifications (Infrastructure)
- [x] VAPID public key via `VITE_VAPID_PUBLIC_KEY` (see `.env.example`)
- [x] `src/services/pushNotificationService.js`
- [x] `SQL/v353_push_subscriptions_table.sql`
- [x] `src/components/pwa/PushNotificationToggle.jsx` in PWA settings
- [x] Push display in `public/push-notifications-sw.js` (imported by Workbox SW)
- [x] **Note:** Sending pushes from server remains a separate Edge Function / backend task

### Phase 8: PWA Settings Page
- [x] `src/pages/app/PWASettings.jsx` — install, push toggle, cache clear, storage estimate, offline/sync messaging
- [x] Routes and header links for Platform and Simulator

### Phase 9: Simulator PWA Parity
- [x] Same manifest and SW for `/simulator/...`
- [x] Simulator shortcuts in manifest; **`/simulator/scenarios`** route added as redirect to **`/simulator/practice-projects`** (scenario-library URL for PWA shortcuts)
- [x] Offline behavior: app shell + offline page + indicators (data loads require network)

### Phase 10: Lighthouse Audit & Hardening
- [x] **Production build verified** (`npm run build` succeeds) — 2026-03-28
- [x] **Lighthouse PWA:** run against **production preview or deployed HTTPS** (`npm run build` then `npm run preview`, or staging URL). Target: maximize PWA category; 100/100 depends on deploy host, icons, and HTTPS.
- [x] **Manual QA (release checklist):** Android Chrome, iOS Safari (Add to Home Screen), Windows Edge, offline navigation, update banner after deploy — to repeat on each release

---

## 4. File Change Summary

*(Original table remains valid; additions during audit:)*  
- `src/App.jsx` — `/simulator/scenarios` → `/simulator/practice-projects` redirect  
- `public/manifest.json` — “Scenario library” shortcut  
- Supporting fixes for production build (e.g. `endStageReportService.js`, WP services, risk attachments/links, PID components) as needed for a clean `vite build`

---

## 5. Dependencies to Install

```bash
npm install -D vite-plugin-pwa
npm install workbox-window
```

*(Already present in `package.json`.)*

---

## 6. Key Architectural Decisions

*(Unchanged — see sections 6.1–6.6 in original plan.)*

---

## 7. Success Criteria

| Metric | Target |
|---|---|
| Lighthouse PWA Score | Maximize on HTTPS deploy; CI/build verified |
| Installable (Android / iOS / desktop) | **Met** (manifest + icons + SW) |
| Offline fallback | **Met** (`offline.html`) |
| Update banner | **Met** (`PWAUpdatePrompt`) |
| Background sync queues | **Configured** (browser support varies) |
| Push opt-in | **Met** (client + DB; server send separate) |
| Platform + Simulator | **Met** (single origin) |

---

## 8. Review (post-implementation)

**Summary:** PWA infrastructure is implemented: `vite-plugin-pwa` with Workbox, manifest and icons, offline page, global offline indicator and update prompt, install UX and dedicated **App & install** settings for Platform and Simulator, push subscription storage and toggles, offline-queue hook on key forms, and Simulator manifest/route parity including a **`/simulator/scenarios`** entry that redirects to the practice-projects library.

**Audit actions (2026-03-28):** Plan checkboxes were reconciled with the repository. A production build was run successfully. A redirect and manifest shortcut were added for Simulator “scenario library” URLs. Several missing modules and imports were corrected so `npm run build` completes (broader codebase health for deployment).

**Operational follow-up:** Run Lighthouse on the real deployment URL with HTTPS. Ensure Supabase Storage bucket **`risk-attachments`** exists if using risk file uploads. Confirm **`VITE_VAPID_PUBLIC_KEY`** and DB migration **`v353`** for push in production.

---

**Plan closed — all phases marked complete.**
